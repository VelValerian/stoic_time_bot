from aiogram.types import ReplyKeyboardMarkup
from app.keyboards.buttons_reply import *

start_keyboard = ReplyKeyboardMarkup(keyboard=start_buttons, resize_keyboard=True)

begin_keyboard = ReplyKeyboardMarkup(keyboard=begin_buttons, resize_keyboard=True)

stop_keyboard = ReplyKeyboardMarkup(keyboard=stop_buttons, resize_keyboard=True)

activity_keyboard = ReplyKeyboardMarkup(keyboard=activity_buttons, resize_keyboard=True)

rest_keyboard = ReplyKeyboardMarkup(keyboard=rest_buttons, resize_keyboard=True)

health_keyboard = ReplyKeyboardMarkup(keyboard=health_buttons, resize_keyboard=True)

health_sports_keyboard = ReplyKeyboardMarkup(keyboard=health_sports_buttons, resize_keyboard=True)

health_routine_keyboard = ReplyKeyboardMarkup(keyboard=health_routine_buttons, resize_keyboard=True)

health_care_keyboard = ReplyKeyboardMarkup(keyboard=health_care_buttons, resize_keyboard=True)

routine_keyboard = ReplyKeyboardMarkup(keyboard=routine_buttons, resize_keyboard=True)

study_keyboard = ReplyKeyboardMarkup(keyboard=study_buttons, resize_keyboard=True)

job_keyboard = ReplyKeyboardMarkup(keyboard=job_buttons, resize_keyboard=True)

road_keyboard = ReplyKeyboardMarkup(keyboard=road_buttons, resize_keyboard=True)
